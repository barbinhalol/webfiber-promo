# Ultra Pedrão (runtime)
Atendente automático da WebFiber fora do horário (modo sombra por padrão — não envia nada até liberação).
Dados de negócio ficam em `data/privado.tar.gz.enc` (criptografado). Instalação: `bash scripts/install.sh`.
